{{ form_start(form) }}
  <div class="row">
        <div class="col-md-8">
            <div class="row">
                <div class="col-md-6"> {{ form_widget(form.titre|raw) }}</div>
            </div>
        </div>
     </div>
    <div class="row">
        <div class="col-md-10">
            <div class="row">
                <div class="col-md-12"> {{ form_widget(form.contenu|raw,{
                    'attr':{'class':'form-control form-control-sm mb-2'}
                }) }}</div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-8 mt-2">
            <div class="row">
            <button class="btn btn-primary">{{ button_label|default('Valider') }}</button>
            </div>
        </div>
    </div>
{{ form_end(form) }}
